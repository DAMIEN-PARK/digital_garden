---
title: LIBRARY
description: 
date: 2025-04-22
tags:
---

```dataview
TABLE WITHOUT ID
    "![|60](" + cover + ")" as Cover,
    link(file.link, title) as Title,
    join(list(author)) as Author,
    status as Status

FROM #Book
WHERE file.folder = "LIBRARY" and status = "⏳Ready"

SORT file.mtime DESC

```

---
### 최근 수정 도서
```dataview
TABLE WITHOUT ID
    "![|60](" + cover + ")" as Cover,
    link(file.link, title) as Title,
    join(list(author)) as Author,
    status as Status

FROM #Book
WHERE file.folder = "LIBRARY"

SORT file.mtime DESC
LIMIT 20
```

